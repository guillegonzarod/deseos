// Paso 4.1: Creamos la clase 'ListaItem' para manejar el funcionamiento de los items
// de la listas
export class ListaItem {
  // Paso 4.2: Declaramos las propiedades:
  public descripcion: string;
  public completado: boolean;
  // Paso 4.3: Declaramos el constructor para inicializar las propiedades:
  constructor(descripcion: string) {
    this.descripcion = descripcion;
    this.completado = false;
  }
  // Paso 5: Creamos el archivo 'src/models/lista.model.ts' para crear la lista
  // Paso 5.1: Vamos al archivo lista.model.ts
}
